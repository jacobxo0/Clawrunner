# Railway deploy config.
# Når Dockerfile findes, bruger Railway Docker (undgår "Error creating build plan with Railpack").
# Se notes/railway-deploy.md for fuld guide.

[deploy]
startCommand = "bash scripts/railway-start.sh"
healthcheckPath = "/health"
healthcheckTimeout = 30
