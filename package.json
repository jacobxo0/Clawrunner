{
  "name": "openclaw-deploy",
  "version": "1.0.0",
  "private": true,
  "description": "OpenClaw gateway deploy (Railway/VPS)",
  "scripts": {
    "start": "./node_modules/.bin/openclaw gateway run --port ${PORT:-18789}"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "openclaw": "2026.5.6"
  }
}
